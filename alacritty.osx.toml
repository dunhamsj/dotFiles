# Filename: ${XDG_CONFIG_HOME}/alacritty/alacritty.toml
# Soft-linked from ${HOME}/documents_git/dotFiles/alacritty.osx.toml
#
# Modifies attributes for alacritty terminal

general.live_config_reload = true

[font]
size = 16

[keyboard]
bindings = [ { key = "+", mods = "Control", action = "IncreaseFontSize" },
             { key = "-", mods = "Control", action = "DecreaseFontSize" } ]

[terminal.shell]
program = "/usr/local/Cellar/bash/5.2.37/bin/bash"
